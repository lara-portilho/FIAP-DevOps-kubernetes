apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: togglemaster-targeting-service
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/lara-portilho/FIAP-DevOps-kubernetes.git
    targetRevision: main
    path: manifests/targeting-service
  destination:
    server: https://kubernetes.default.svc
    namespace: togglemaster
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
